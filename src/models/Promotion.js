import { Model } from './Base'

class Promotion {
  // uid = String
  code = {
    type: String
  }
  // name = String
  // start = Date
  // end = Date
  // category = Array
  // content = String
}

export default Model(Promotion)
